Installation
************

The package will not be made available on PyPI until a stable API has been attained.
In the meanwhile, pyspatialml can be installed from GitHub directly via:
::

    pip install git+https://github.com/stevenpawley/Pyspatialml

